Исправляющий выпуск FilesConverter.

- устранено зависание после успешного создания файла;
- результаты фоновых задач теперь всегда обрабатываются в главном потоке интерфейса;
- окно «Готово» открывается асинхронно и больше не блокирует цикл обработки событий Windows;
- исправление действует для конвертации, объединения и проверки обновлений;
- добавлены Windows-тесты потока выполнения callback и неблокирующего окна результата.
